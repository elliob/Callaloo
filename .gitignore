# Xcode / Swift
DerivedData/
build/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store

# Local tooling (not for repo)
.tmp-firebase-config/

# Firebase iOS (API keys; download locally — see README / firebase-ios-bootstrap.sh)
Callaloo/GoogleService-Info.plist

# Functions
functions/node_modules/
functions/lib/
# Firebase CLI parameterized config / secrets (e.g. RESEND_API_KEY)
functions/.env.*
.vercel
.env*.local
